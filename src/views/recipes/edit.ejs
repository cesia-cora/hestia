<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Edit your recipe | Hestia Cooking</title>
</head>
<body>
    <%- include('../partials/nav') %>

    <div class="main">
      <br>
        <h5 class="titulo">Edit recipe</h5>
  
        <form action="/recipes/<%= recipe.id %>/update?_method=PUT" method="post">
          <div class="mb-3 row">
            <label for="title" class="col-sm-2 col-form-label">Title:</label>
            <div class="col-sm-8">
              <input type="text" class="form-control" name="title" id="titulo" value="<%= recipe.title %>"/>
            </div>
          </div>
          <div class="mb-3 row">
            <label for="contenido" class="col-sm-2 col-form-label">Content:</label>
            <div class="col-sm-8">
              <textarea class="form-control" name="content" id="content" rows="10"> <%= recipe.content %></textarea>
            </div>
          </div>
          <div class="mb-3 row">
            <label for="contenido" class="col-sm-2 col-form-label">Category:</label>
            <div class="col-sm-8">
              <input type="text" class="form-control" list="categories" name="category" id="exampleDataList" placeholder="Search a category..." value="<%= recipe.category %>">
              <datalist id="categories">
                <option value="Pies">
                <option value="Alfajores">
                <option value="Cookies">
                <option value="Mousse">
                <option value="Custard">
                <option value="Ice Cream">
                <option value="Muffins">
              </datalist>
            </div>
          </div>
          <button type="submit" class="btn btn-danger btn-lg">Save changes</button>
          <a href="/recipes/<%= recipe.id %>" class="btn btn-warning btn-lg">Go back</a>
        </form>
      </div>

    <%- include('../partials/footer') %>
</body>
</html>